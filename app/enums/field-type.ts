export enum FieldType {
    TEXT = 'text',
    NUMBER = 'number',
    SELECT = 'select',
    CHECKBOX = 'checkbox',
    RADIO = 'radio',
    DATE = 'date',
    TIME = 'time',
    DATETIME = 'datetime',
    EMAIL = 'email',
    PASSWORD = 'password',
    URL = 'url',
}